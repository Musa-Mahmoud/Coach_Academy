/*
#include <iostream>

using namespace std;

int main()
{
    long long n, k;

    cin >> n >> k;

    if( k > ( (n + 1) >> 1) && n % 2 == 0 )
    {
        cout << (2 * k - n) << "\n";
    }
    else if( k > ( (n + 1) >> 1) && n % 2 == 1 )
    {
        cout << (2 * k - n - 1) << "\n";
    }
    else
    {
        cout << (2 * k - 1) << "\n";
    }

    return 0;
}
*/
